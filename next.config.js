/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Si vous aviez des configurations spécifiques dans votre .ts, ajoutez-les ici
}

module.exports = nextConfig